context:
  name: cython-cmake
  version: "0.3.0"
  username: scikit-build

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/${{ username }}/${{ name|lower }}/archive/refs/tags/v${{ version }}.tar.gz
  sha256: caca45d7c76e6c480a8e7bfb6558d24ca79b97829fbbfab5546ce559b35ddc37

build:
  script: ${{ PYTHON }} -m pip install --no-build-isolation --no-deps -vv .
  number: 0
  noarch: python

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - hatch-vcs
    - pip
  run:
    - python >=${{ python_min }}
    - importlib_resources
    - cython

tests:
  - script:
      - python -m pytest --durations=20
    files:
      source:
        - tests/
        - src/
        - pyproject.toml
    requirements:
      run:
        - ${{ compiler("c") }}
        - ${{ compiler("cxx") }}
        - ${{ stdlib("c") }}
        - python ${{ python_min }}.*
        - pytest
        - scikit-build-core
        - ninja
        - cmake >=3.21
        - make


about:
  homepage: https://github.com/scikit-build/cython-cmake
  summary: CMake helpers for building Cython modules
  license: Apache-2.0
  license_file: LICENSE
  repository: https://github.com/scikit-build/cython-cmake

extra:
  recipe-maintainers:
    - conda-forge/scikit-build
